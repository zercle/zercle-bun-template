apiVersion: apps/v1
kind: Deployment
metadata:
  name: zercle-bun-template
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: zercle-bun-template
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zercle-bun-template
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: server
          image: zercle-bun-template:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: zercle-bun-template-config
            - secretRef:
                name: zercle-bun-template-secret
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 65532
            capabilities:
              drop: ["ALL"]
