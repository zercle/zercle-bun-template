apiVersion: v1
kind: Service
metadata:
  name: zercle-bun-template
spec:
  selector:
    app.kubernetes.io/name: zercle-bun-template
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
