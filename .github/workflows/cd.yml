# =============================================================================
# GitHub Actions CD Workflow
# =============================================================================
# This workflow handles continuous deployment of container images.
#
# Triggers:
#   - Push of a version tag (v*)
#
# Jobs:
#   - build-and-push: multi-arch (amd64/arm64) server + migrate images to ghcr.io
# =============================================================================

name: CD

on:
  push:
    tags: ["v*"]

jobs:
  # ==========================================================================
  # Job: Build and Push Container Image
  # ==========================================================================
  build-and-push:
    name: Build & Push Container
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Extract metadata
        id: meta
        run: |
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "dev")
          COMMIT_SHA=$(git rev-parse --short HEAD)
          BUILD_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          {
            echo "version=${VERSION}"
            echo "commit_sha=${COMMIT_SHA}"
            echo "build_time=${BUILD_TIME}"
          } >> "$GITHUB_OUTPUT"

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push server image with docker/build-push-action
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Containerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:${{ steps.meta.outputs.version }}
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          build-args: |
            APP_VERSION=${{ steps.meta.outputs.version }}
            APP_COMMIT_SHA=${{ steps.meta.outputs.commit_sha }}
            APP_BUILD_TIME=${{ steps.meta.outputs.build_time }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push migrate image with docker/build-push-action
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Containerfile.migrate
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-migrate:${{ steps.meta.outputs.version }}
            ghcr.io/${{ github.repository }}-migrate:latest
            ghcr.io/${{ github.repository }}-migrate:${{ github.ref_name }}
          build-args: |
            APP_VERSION=${{ steps.meta.outputs.version }}
            APP_COMMIT_SHA=${{ steps.meta.outputs.commit_sha }}
            APP_BUILD_TIME=${{ steps.meta.outputs.build_time }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
